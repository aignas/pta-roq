// This will be reimplementation of the thing in C++

/*
 * This is a class for unit vectors. I have a feeling, that the p vector might be not
 * necessary here
 */

#include <cmath>
#include <vector>
#include <cav/Vec3.hh>
#include <signal.hh>

PulsarGrid:: PulsarGrid (int N, std::vector<double> ranges, std::vector<double> noise) {
    // randomize mR
    // randomize mNoise
    // transform the range and noise parameters so that we have proper arrays
}

getNoise (int idx) {
}

getWhiteNoise (int idx) {
}

getRedNoise   (int idx) {
}

getPowLawNoise(int idx) {
}

getAngles (int idx) {
}

getUnitVector (int idx) {
}
